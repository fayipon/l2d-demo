{
  "name": "l2d-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "fixture": "node scripts/build-fixture.mjs"
  },
  "comment": [
    "The three runtime versions are pinned EXACTLY, with no caret, and they must",
    "match project01's installed versions. This tool exists to answer 'will this",
    "model run in the game', and a bench on a newer runtime would cheerfully load",
    "a model the game then refuses. If project01's versions move, move these with",
    "them -- see README.md."
  ],
  "dependencies": {
    "@pixi/sound": "5.2.3",
    "pixi-live2d-display-advanced": "1.1.0",
    "pixi.js": "7.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "sharp": "^0.35.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
